name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main

# Concurrency group para evitar despliegues simultáneos
concurrency:
  group: "pages"
  cancel-in-progress: false

# Permisos especiales para el GITHUB_TOKEN
permissions:
  contents: read
  pages: write      # Necesario para actions/deploy-pages
  id-token: write   # Necesario para actions/deploy-pages

jobs:
  # Job para construir el sitio
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Set up Node.js 🏗️
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Dependencies 📦
        run: npm install

      - name: Build Website 🛠️
        run: npm run build

      - name: Upload artifact 📦
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  # Job para desplegar el sitio
  deploy:
    needs: build
    runs-on: ubuntu-latest
    
    # Permisos específicos para el job de deploy
    permissions:
      pages: write
      id-token: write
    
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      - name: Deploy to GitHub Pages 🚀
        id: deployment
        uses: actions/deploy-pages@v4
